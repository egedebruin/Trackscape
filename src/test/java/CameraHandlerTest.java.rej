diff a/src/test/java/CameraHandlerTest.java b/src/test/java/CameraHandlerTest.java	(rejected hunks)
@@ -24,11 +24,16 @@
         // so if the main changes this should be included.
         // Load OpenCV library.
         System.load(System.getProperty("user.dir")
-            + File.separator + "libs" + File.separator + "opencv_ffmpeg341_64.dll");
+            + File.separator + "libs" + File.separator
+            + "opencv_ffmpeg341_64.dll");
         System.load(System.getProperty("user.dir")
-            + File.separator + "libs" + File.separator + "opencv_java341.dll");
+            + File.separator + "libs" + File.separator
+            + "opencv_java341.dll");
     }
 
+    /**
+     * Test if camera is added to the list.
+     */
     @Test
     void addCameraTest() {
         CameraHandler c = new CameraHandler();
